// alx-react-app-props/src/UserContext.js
import React from 'react';

// We make a special secret box for our user's info
const UserContext = React.createContext();

export default UserContext; // We send this box out so others can use it